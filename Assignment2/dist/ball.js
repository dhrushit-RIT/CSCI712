class Ball extends THREE.Mesh {
    constructor(radius, geometry_ball, material_ball) {
        super(geometry_ball, material_ball);
        this.radius = radius;
    }
}
//# sourceMappingURL=Ball.js.map